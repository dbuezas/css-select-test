# Test code for CSS-Select pull request.

* pull repo
* make sure 'css-select-patched' submodule gets pulled (`git submodule update --recursive --remote
`)
* run `npm run build` to build main.js
* open `test.html` file in browser.

# results:
![](table.png)

